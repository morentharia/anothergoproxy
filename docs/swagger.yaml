info:
  contact: {}
  license: {}
paths:
  /config:
    get:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: answer
          schema:
            type: string
  /infoPages:
    get:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: answer
          schema:
            type: string
  /log:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: answer
          schema:
            type: string
  /navigatePage:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: answer
          schema:
            type: string
  /reloadPage:
    get:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: answer
          schema:
            type: string
swagger: "2.0"
